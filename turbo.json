{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "//#format": {},
    "//#format:check": {},
    "lint": {},
    "test": {
      "dependsOn": ["//#format:check", "build", "lint"]
    }
  }
}
